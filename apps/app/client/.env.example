# Where the marketing site lives. Used for "← Home" link from sign-in.
VITE_MARKETING_URL=http://localhost:4322

# (Optional) backend API base URL. Leave blank if same origin (Vite proxy in dev).
# VITE_API_URL=

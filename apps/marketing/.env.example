# Public env vars (must be PUBLIC_ prefixed in Astro)
# Where the actual app lives. Used for "Sign in" / "Get started" CTAs.
PUBLIC_APP_URL=http://localhost:5173
# Where the API lives. Used for magic-link and contact form submissions.
PUBLIC_API_URL=http://localhost:8787
